name: xyz_gen
description:
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.0.5

dependency_overrides:
  xyz_utils:
    path: ../xyz_utils

dependencies:
  analyzer:
  args: ^2.4.2
  path:

  xyz_utils:
    git:
      url: https://github.com/robmllze/xyz_utils.git
      ref: master

dev_dependencies:
  lints: ^2.0.0
